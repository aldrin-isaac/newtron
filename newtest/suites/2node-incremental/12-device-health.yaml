name: device-health
description: Run health checks on all devices after BGP convergence
topology: 2node
platform: sonic-vpp
requires: [bgp-converge]

steps:
  - name: health-check-all
    action: verify-health
    devices: all
