name: cleanup
description: Run cleanup on leaf1 to remove orphaned resources and verify
topology: 2node
requires: [service-churn]

steps:
  - name: run-cleanup
    action: cleanup
    devices: [leaf1]

  - name: verify-no-orphan-bindings
    action: verify-config-db
    devices: [leaf1]
    table: NEWTRON_SERVICE_BINDING
    expect:
      min_entries: 0
