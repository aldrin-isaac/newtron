name: cleanup
description: Final cleanup pass - remove any remaining orphaned config
topology: 3node
platform: sonic-vpp
requires: [teardown-verify]

steps:
  - name: cleanup-leafs
    action: cleanup
    devices: [leaf1, leaf2]
