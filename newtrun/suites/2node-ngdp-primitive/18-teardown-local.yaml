name: teardown-local
description: |
  Reverse teardown of local primitives (Phase 2) using newtron primitives.
  Removes SVI, VLAN members, VLAN, interface IPs, VRF interface binding, and VRF
  in reverse order of setup. Frees Ethernet1/Ethernet3 on sw1 and Ethernet1 on sw2
  for reuse in cross-switch routing phase.
topology: 2node-ngdp
requires: [irb, static-route]

steps:
  # 1. Remove SVI from VLAN 100
  - name: remove-svi-vlan100
    action: newtron
    devices: [switch1]
    method: POST
    url: /node/{{device}}/unconfigure-irb
    params: {vlan_id: 100}

  - name: verify-svi-removed
    action: newtron
    devices: [switch1]
    url: /node/{{device}}/configdb/VLAN_INTERFACE/Vlan100/exists
    expect:
      jq: '.exists == false'

  # 2. Remove VLAN members
  - name: unconfigure-bridged-ethernet2
    action: newtron
    devices: [switch1]
    method: POST
    url: /node/{{device}}/interface/Ethernet2/unconfigure-interface

  - name: unconfigure-bridged-ethernet3
    action: newtron
    devices: [switch1]
    method: POST
    url: /node/{{device}}/interface/Ethernet3/unconfigure-interface

  - name: unconfigure-bridged-ethernet1
    action: newtron
    devices: [switch1]
    method: POST
    url: /node/{{device}}/interface/Ethernet1/unconfigure-interface

  - name: verify-member-eth1-removed
    action: newtron
    devices: [switch1]
    url: /node/{{device}}/configdb/VLAN_MEMBER/Vlan100%7CEthernet1/exists
    expect:
      jq: '.exists == false'

  # 3. Delete VLAN 100
  - name: delete-vlan100
    action: newtron
    devices: [switch1]
    method: POST
    url: /node/{{device}}/delete-vlan
    params: {id: 100}

  - name: verify-vlan100-removed
    action: newtron
    devices: [switch1]
    url: /node/{{device}}/configdb/VLAN/Vlan100/exists
    expect:
      jq: '.exists == false'

  # 4. Unconfigure sw2:Ethernet1 (removes IP + VRF binding via intent)
  - name: unconfigure-ethernet1
    action: newtron
    devices: [switch2]
    method: POST
    url: /node/{{device}}/interface/Ethernet1/unconfigure-interface

  - name: verify-ip-removed
    action: newtron
    devices: [switch2]
    url: /node/{{device}}/configdb/INTERFACE/Ethernet1%7C10.20.1.1%2F31/exists
    expect:
      jq: '.exists == false'

  # 6. Delete Vrf_local
  - name: delete-vrf-local
    action: newtron
    devices: [switch2]
    method: POST
    url: /node/{{device}}/delete-vrf
    params: {name: Vrf_local}

  - name: verify-vrf-removed
    action: newtron
    devices: [switch2]
    url: /node/{{device}}/configdb/VRF/Vrf_local/exists
    expect:
      jq: '.exists == false'
