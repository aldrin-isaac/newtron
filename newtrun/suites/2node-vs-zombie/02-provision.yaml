name: provision
description: |
  Provision both switches via topology provisioner. Establishes baseline
  CONFIG_DB state before injecting a synthetic zombie intent.
topology: 2node-vs-service
requires: [boot-ssh]

steps:
  - name: provision-switches
    action: provision
    devices: [switch1, switch2]

  - name: config-reload
    action: newtron
    devices: [switch1, switch2]
    method: POST
    url: /node/{{device}}/config-reload

  - name: restart-bgp
    action: newtron
    devices: [switch1, switch2]
    method: POST
    url: /node/{{device}}/restart-service
    params: {service: bgp}

  - name: wait-bgp-instance
    action: newtron
    devices: [switch1, switch2]
    method: POST
    url: /node/{{device}}/ssh-command
    params: {command: "sudo vtysh -c 'show running-config'"}
    poll:
      timeout: 30s
      interval: 3s
    expect:
      jq: '.output | contains("router bgp")'

  - name: verify-bgp
    action: newtron
    devices: [switch1, switch2]
    url: /node/{{device}}/bgp/check
    poll:
      timeout: 120s
      interval: 5s
    expect:
      jq: 'length > 0 and all(.[]; .status == "pass")'
