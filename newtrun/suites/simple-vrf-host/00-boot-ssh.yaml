name: boot-ssh
description: Verify SSH connectivity to switch2
topology: 2node-ngdp

steps:
  - name: ssh-echo-switch2
    action: ssh-command
    devices: [switch2]
    command: "echo ok"
    expect:
      contains: "ok"
